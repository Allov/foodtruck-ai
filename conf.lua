function love.conf(t)
    t.title = "My Game"        -- Game window title
    t.version = "11.4"         -- LÖVE version
    t.window.width = 800       -- Window width
    t.window.height = 600      -- Window height
    
    -- For debugging
    t.console = true
end